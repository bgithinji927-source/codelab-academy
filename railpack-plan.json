{
  "version": 1,
  "build": {
    "commands": [
      "npm install --prefer-offline --no-audit --no-fund",
      "npm run build"
    ],
    "env": {
      "NPM_CONFIG_PRODUCTION": "false"
    }
  },
  "run": {
    "command": "npm start",
    "env": {}
  }
}
